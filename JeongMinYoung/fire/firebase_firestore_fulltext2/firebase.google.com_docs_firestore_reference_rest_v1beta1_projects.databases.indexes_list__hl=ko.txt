Source URL: https://firebase.google.com/docs/firestore/reference/rest/v1beta1/projects.databases.indexes/list?hl=ko

## 본문
Lists the indexes that match the specified filters.

GET https://firestore.googleapis.com/v1beta1/{parent=projects/*/databases/*}/indexes

The URL uses gRPC Transcoding syntax.

The request body must be empty.

The response for FirestoreAdmin.ListIndexes.

If successful, the response body contains data with the following structure:

Requires one of the following OAuth scopes:

https://www.googleapis.com/auth/datastore

https://www.googleapis.com/auth/cloud-platform

For more information, see the OAuth 2.0 Overview.

### 표
| Parameters | |
| --- | --- |
| Parameters | |
| parent | string The database name. For example: projects/{projectId}/databases/{databaseId} |

| Parameters | |
| --- | --- |
| Parameters | |
| filter | string |
| pageSize | integer The standard List page size. |
| pageToken | string The standard List page token. |

| JSON representation |
| --- |
| JSON representation |
| { "indexes": [ { object (Index) } ], "nextPageToken": string } |

| Fields | |
| --- | --- |
| Fields | |
| indexes[] | object (Index) The indexes. |
| nextPageToken | string The standard List next-page token. |

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
GET https://firestore.googleapis.com/v1beta1/{parent=projects/*/databases/*}/indexes

언어: 코드 · 셀#2
FirestoreAdmin.ListIndexes

언어: 코드 · 셀#3
https://www.googleapis.com/auth/datastore

언어: 코드 · 셀#4
https://www.googleapis.com/auth/cloud-platform