Source URL: https://firebase.google.com/docs/firestore/client/query-data?hl=ko

## 서문
Cloud Firestore는 컬렉션 또는 컬렉션 그룹에서 검색할 문서를 지정할 수 있는 강력한 쿼리 기능을 제공합니다. 데이터 가져오기 및 실시간 업데이트 가져오기의 설명대로 이러한 쿼리를 get() 또는 addSnapshotListener()와 함께 사용할 수도 있습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
get()

언어: 코드 · 셀#2
addSnapshotListener()

## 예시 데이터
우선 도시에 관한 데이터를 작성하고 다양한 방식으로 이 데이터를 불러오는 방법에 대해 살펴보세요.

## 간단한 쿼리
다음 쿼리는 주가 CA인 모든 도시를 반환합니다.

다음 쿼리는 모든 주도를 반환합니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
CA

## 쿼리 실행
쿼리 객체를 만든 후 get() 함수를 사용하여 결과를 검색합니다.

쿼리 결과 검색에 대한 자세한 내용은 데이터 가져오기를 참조하세요. 쿼리에 리스너를 추가하면 현재 결과를 가져오고 향후 업데이트도 수신할 수 있습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
get()

## 쿼리 연산자
where() 메서드는 필터링할 필드, 비교 연산자, 값의 3가지 매개변수를 사용합니다. Cloud Firestore는 다음 비교 연산자를 지원합니다.

< 미만

<= 작거나 같음

== 같음

> 보다 큼

>= 이상

!= 같지 않음

array-contains

array-contains-any

in

not-in

예를 들면 다음과 같습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
where()

언어: 코드 · 셀#1
<

언어: 코드 · 셀#2
<=

언어: 코드 · 셀#3
==

언어: 코드 · 셀#4
>

언어: 코드 · 셀#5
>=

언어: 코드 · 셀#6
!=

언어: 코드 · 셀#7
array-contains

언어: 코드 · 셀#8
array-contains-any

언어: 코드 · 셀#9
in

언어: 코드 · 셀#10
not-in

## 같지 않음(!=)
지정된 필드가 존재하고 비교 값과 일치하지 않는 문서를 반환하려면 같지 않음(!=) 연산자를 사용합니다. 예를 들면 다음과 같습니다.

이 쿼리는 capital 필드에 false 또는 null 이외의 값이 포함된 모든 city 문서를 반환합니다. 여기에는 capital 필드 값이 true이거나 null 이외의 모든 비부울 값과 일치하는 city 문서가 포함됩니다.

이 쿼리는 capital 필드가 없는 city 문서를 반환하지 않습니다. 같지 않음(!=)과 not-in 쿼리는 지정된 필드가 없는 문서를 제외합니다.

빈 문자열(""), null, NaN(숫자 아님)을 포함하는 어떤 값으로든 설정되면 필드가 존재합니다. x != null이 undefined로 평가되므로 null 필드 값은 != 절과 일치하지 않습니다.

!= 쿼리에는 다음과 같은 제한사항이 있습니다.

지정된 필드가 있는 문서만 쿼리와 일치할 수 있습니다.

not-in과 !=을 복합 쿼리에 결합할 수 없습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
!=

언어: 코드 · 셀#1
capital

언어: 코드 · 셀#2
false

언어: 코드 · 셀#3
null

언어: 코드 · 셀#4
city

언어: 코드 · 셀#5
true

언어: 코드 · 셀#2
city

언어: 코드 · 셀#3
!=

언어: 코드 · 셀#4
not-in

언어: 코드 · 셀#1
""

언어: 코드 · 셀#2
null

언어: 코드 · 셀#3
NaN

언어: 코드 · 셀#4
x != null

언어: 코드 · 셀#5
undefined

언어: 코드 · 셀#6
!=

언어: 코드 · 셀#1
not-in

언어: 코드 · 셀#2
!=

## 배열 멤버십
array-contains 연산자를 사용하면 배열 값 기준으로 필터링할 수 있습니다. 예를 들면 다음과 같습니다.

이 쿼리는 모든 city 문서를 반환합니다. 여기에서 regions 필드는 west_coast를 포함하는 배열입니다. 배열에 쿼리하는 값의 인스턴스가 여러 개 있으면 문서가 결과에 한 번만 포함됩니다.

분리(or 그룹)당 최대 1개의 array-contains 절을 사용할 수 있습니다. 동일한 분리에서 array-contains와 array-contains-any를 결합할 수 없습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
array-contains

언어: 코드 · 셀#1
city

언어: 코드 · 셀#2
regions

언어: 코드 · 셀#3
west_coast

언어: 코드 · 셀#1
or

언어: 코드 · 셀#2
array-contains

언어: 코드 · 셀#3
array-contains-any

## in, not-in, array-contains-any
in 연산자를 사용하여 같은 필드에서 등호(==) 절을 최대 30개까지 논리적 OR과 결합합니다. in 쿼리는 지정된 필드가 비교 값과 일치하는 문서를 반환합니다. 예를 들면 다음과 같습니다.

이 쿼리는 country 필드가 USA 또는 Japan으로 설정된 모든 city 문서를 반환합니다. 예시 데이터에서는 여기에 SF, LA, DC, TOK 문서가 포함됩니다.

not-in 연산자를 사용하여 동일한 필드에서 같지 않은(!=) 절을 최대 10개까지 논리적 AND와 결합합니다. not-in 쿼리는 지정된 필드가 존재하고 null이 아니며 어떤 비교 값과도 일치하지 않는 문서를 반환합니다. 예를 들면 다음과 같습니다.

이 쿼리는 country 필드가 존재하고 USA, Japan 또는 null로 설정되지 않은 모든 city 문서를 반환합니다. 예시 데이터에서는 여기에 London 및 Hong Kong 문서가 포함됩니다.

not-in 쿼리는 지정된 필드가 없는 문서를 제외합니다. 빈 문자열(""), null, NaN(숫자 아님)을 포함하는 어떤 값으로든 설정되면 필드가 존재합니다. x != null은 undefined로 평가됩니다. null을 비교 값 중 하나로 가지고 있는 not-in 쿼리는 어떤 문서와도 일치하지 않습니다.

마찬가지로 array-contains-any 연산자를 사용하여 같은 필드에서 array-contains 절을 최대 30개까지 논리적 OR과 결합합니다. array-contains-any 쿼리는 문서를 반환합니다. 여기서 지정된 필드는 비교 값이 1개 이상 포함된 배열입니다.

이 쿼리는 모든 city 문서를 반환합니다. 여기서 regions 필드는 west_coast 또는 east_coast가 포함된 배열입니다. 예시 데이터에서는 여기에 SF, LA, DC 문서가 포함됩니다.

중복되는 array-contains-any의 결과가 삭제됩니다. 문서의 배열 필드가 비교 값 두 개 이상과 일치하더라도 결과 집합에는 문서가 한 번만 포함됩니다.

array-contains-any는 항상 배열 데이터 유형 기준으로 필터링합니다. 예를 들어 위 쿼리는 배열 대신 regions 필드가 west_coast 문자열인 city 문서를 반환하지 않습니다.

배열 값을 in의 비교 값으로 사용할 수 있지만 array-contains-any와 달리 절은 배열의 길이, 순서, 값과 정확하게 일치합니다. 예를 들면 다음과 같습니다.

이 쿼리는 모든 city 문서를 반환합니다. 여기서 regions 필드는 west_coast 또는 east_coast의 요소 하나가 정확하게 포함된 배열입니다. 예시 데이터에서는 DC 문서만 ["east_coast"]의 regions 필드에 해당합니다. 그러나 regions 필드가 ["west_coast", "norcal"]이므로 SF 문서가 일치하지 않습니다.

in, not-in, array-contains-any의 다음 제한사항에 유의하세요.

Cloud Firestore는 or, in, array-contains-any 연산자를 통해 논리적 OR 쿼리를 지원합니다. 이러한 쿼리는 쿼리의 논리합 표준형을 기반으로 분리 30개로 제한됩니다.

분리(or 그룹)당 최대 1개의 array-contains 절을 사용할 수 있습니다. 동일한 분리에서 array-contains와 array-contains-any를 결합할 수 없습니다.

not-in과 !=을 결합할 수 없습니다.

not-in은 비교 값을 최대 10개까지 지원합니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
in

언어: 코드 · 셀#2
==

언어: 코드 · 셀#3
OR

언어: 코드 · 셀#1
country

언어: 코드 · 셀#2
USA

언어: 코드 · 셀#3
Japan

언어: 코드 · 셀#4
city

언어: 코드 · 셀#5
SF

언어: 코드 · 셀#6
LA

언어: 코드 · 셀#7
DC

언어: 코드 · 셀#8
TOK

언어: 코드 · 셀#1
not-in

언어: 코드 · 셀#2
!=

언어: 코드 · 셀#3
AND

언어: 코드 · 셀#4
null

언어: 코드 · 셀#5
city

언어: 코드 · 셀#6
London

언어: 코드 · 셀#7
Hong Kong

언어: 코드 · 셀#2
""

언어: 코드 · 셀#3
null

언어: 코드 · 셀#4
NaN

언어: 코드 · 셀#5
x != null

언어: 코드 · 셀#6
undefined

언어: 코드 · 셀#1
array-contains-any

언어: 코드 · 셀#2
array-contains

언어: 코드 · 셀#1
regions

언어: 코드 · 셀#2
west_coast

언어: 코드 · 셀#3
east_coast

언어: 코드 · 셀#4
SF

언어: 코드 · 셀#5
LA

언어: 코드 · 셀#6
DC

언어: 코드 · 셀#2
regions

언어: 코드 · 셀#3
west_coast

언어: 코드 · 셀#2
array-contains-any

언어: 코드 · 셀#4
DC

언어: 코드 · 셀#5
["east_coast"]

언어: 코드 · 셀#6
["west_coast", "norcal"]

언어: 코드 · 셀#7
SF

언어: 코드 · 셀#2
not-in

언어: 코드 · 셀#3
array-contains-any

언어: 코드 · 셀#1
or

언어: 코드 · 셀#2
in

언어: 코드 · 셀#4
OR

언어: 코드 · 셀#5
array-contains

언어: 코드 · 셀#6
not-in

언어: 코드 · 셀#7
!=

## 복합(AND) 쿼리
여러 등호 연산자(== 또는 array-contains)를 연결하여 제약조건을 논리적 AND와 결합할 수 있습니다. 하지만 등호 연산자와 부등호 연산자(<, <=, >, !=)를 결합하려면 복합 색인을 만들어야 합니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
==

언어: 코드 · 셀#2
array-contains

언어: 코드 · 셀#3
AND

언어: 코드 · 셀#4
<

언어: 코드 · 셀#5
<=

언어: 코드 · 셀#6
>

언어: 코드 · 셀#7
!=

## 쿼리 OR회
제약 조건을 논리적 OR과 결합할 수 있습니다. 예를 들면 다음과 같습니다.

Cloud Firestore는 복합 색인을 사용하여 OR 쿼리를 제공합니다. 색인이 쿼리를 지원하지 않는 경우 Cloud Firestore는 데이터베이스에 대한 추가 색인을 제안합니다.

OR 쿼리와 복합 쿼리를 결합하여 OR 및 AND 작업의 조합을 필터링할 수 있습니다. 예를 들면 다음과 같습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
OR

언어: 코드 · 셀#2
AND

## 제한사항
or 쿼리에는 다음과 같은 제한사항이 있습니다.

Cloud Firestore는 쿼리의 논리합 표준형을 기반으로 최대 30개의 분리로 쿼리를 제한합니다. 여러 OR 그룹의 AND를 수행할 때 이 한도에 도달할 가능성이 높습니다.

동일한 쿼리에서 not-in과 in, array-contains-any 또는 or을 결합할 수 없습니다.

제한사항에 대한 자세한 설명은 쿼리 제한사항을 참조하세요.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
or

언어: 코드 · 셀#1
OR

언어: 코드 · 셀#2
AND

언어: 코드 · 셀#3
not-in

언어: 코드 · 셀#4
in

언어: 코드 · 셀#5
array-contains-any

언어: 코드 · 셀#6
or

## 컬렉션 그룹 쿼리
컬렉션 그룹은 ID가 동일한 모든 컬렉션으로 구성됩니다. 기본적으로 쿼리는 데이터베이스의 단일 컬렉션에서 결과를 검색합니다. 단일 컬렉션 대신 컬렉션 그룹에서 문서를 검색하려면 컬렉션 그룹 쿼리를 사용하세요.

예를 들어 각 도시에 명소 하위 컬렉션을 추가하여 landmarks 컬렉션 그룹을 만들 수 있습니다.

앞에서 설명한 단순 쿼리와 복합 쿼리를 사용하여 한 도시의 landmarks 하위 컬렉션을 쿼리할 수 있지만 모든 도시의 landmarks 컬렉션에서 결과를 한 번에 검색할 수도 있습니다.

landmarks 컬렉션 그룹은 ID가 landmarks인 모든 컬렉션으로 구성되며, 컬렉션 그룹 쿼리를 사용하여 쿼리할 수 있습니다. 예를 들어 이 컬렉션 그룹 쿼리는 모든 도시의 모든 museum 명소를 검색합니다.

컬렉션 그룹 쿼리를 사용하기 전에 컬렉션 그룹 쿼리를 지원하는 색인을 만들어야 합니다. 오류 메시지, 콘솔 또는 Firebase CLI를 통해 색인을 생성할 수 있습니다.

웹 및 모바일 SDK의 경우 컬렉션 그룹 쿼리를 허용하는 규칙을 생성해야 합니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
landmarks

언어: 코드 · 셀#2
museum

## 쿼리 성능 설명
Cloud Firestore를 사용하면 백엔드에서 쿼리의 성능을 측정하고 백엔드 쿼리 실행에 대한 자세한 성능 통계를 수신합니다.

Query Explain 결과는 쿼리가 실행되는 방식을 이해하는 데 도움이 되므로 비효율성과 서버 측 병목 현상이 발생할 가능성이 있는 위치를 보여줍니다.

자세한 내용은 Query Explain 가이드를 참조하세요.

## 쿼리 제한사항
다음 목록에는 Cloud Firestore 쿼리 제한사항이 요약되어 있습니다.

Cloud Firestore는 or, in, array-contains-any 연산자를 통해 논리적 OR 쿼리를 지원합니다. 이러한 쿼리는 쿼리의 논리합 표준형을 기반으로 분리 30개로 제한됩니다.

분리(or 그룹)당 최대 1개의 array-contains 절을 사용할 수 있습니다. 동일한 분리에서 array-contains와 array-contains-any를 결합할 수 없습니다.

동일한 쿼리에서 not-in과 in, array-contains-any 또는 or을 결합할 수 없습니다.

쿼리당 하나의 not-in 또는 !=만 허용됩니다.

not-in은 비교 값을 최대 10개까지 지원합니다.

쿼리에서 필터, 정렬 순서, 상위 문서 경로(하위 컬렉션은 1, 루트 컬렉션은 0)의 합이 100을 초과할 수 없습니다. 이는 쿼리의 논리합 표준형을 기반으로 계산됩니다.

필드에 부등식 필터가 있는 쿼리는 해당 필드를 기준으로 정렬하고 해당 필드의 존재 여부를 필터링합니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
or

언어: 코드 · 셀#2
in

언어: 코드 · 셀#3
array-contains-any

언어: 코드 · 셀#4
OR

언어: 코드 · 셀#5
array-contains

언어: 코드 · 셀#6
not-in

언어: 코드 · 셀#7
!=

## OR 쿼리 한도
Cloud Firestore는 쿼리의 계산 비용이 너무 많아지지 않도록 결합할 수 있는 AND 및 OR 절 수를 제한합니다. 이 한도를 적용하기 위해 Cloud Firestore는 논리적 OR 작업을 수행하는 쿼리(or, in, array-contains-any)를 논리합 표준형(AND의 OR라고도 알려짐)으로 변환합니다. Cloud Firestore는 논리합 표준형을 기반으로 최대 30개의 분리로 쿼리를 제한합니다.

Cloud Firestore는 다음 두 규칙을 적용하여 쿼리를 논리합 표준형으로 변환합니다.

Flatten

주어진 조건 A, B, C:

A and (B and C) => A and B and C

Flatten

주어진 조건 A, B, C:

A and (B and C) => A and B and C

분산법

주어진 조건 A, B, C, D:

A and (B or C) => (A and B) or (A and C)
(A or B) and (C or D) => (A and C) or (A and D) or (B and C) or (B and D)

분산법

주어진 조건 A, B, C, D:

A and (B or C) => (A and B) or (A and C)

(A or B) and (C or D) => (A and C) or (A and D) or (B and C) or (B and D)

이러한 규칙을 in 및 array-contains-any 쿼리에 적용할 때 이러한 연산자가 OR의 약칭이라는 것을 유념하세요. 예를 들어 a in [1,2]는 a = 1 OR a = 2의 약칭입니다.

다음 예시는 여러 쿼리에 대한 분리 횟수를 보여줍니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
AND

언어: 코드 · 셀#2
OR

언어: 코드 · 셀#3
or

언어: 코드 · 셀#4
in

언어: 코드 · 셀#5
array-contains-any

언어: 코드 · 셀#1
A

언어: 코드 · 셀#2
B

언어: 코드 · 셀#3
C

언어: 코드 · 셀#4
A and (B and C) => A and B and C

언어: 코드 · 셀#5
D

언어: 코드 · 셀#6
A and (B or C) => (A and B) or (A and C)

언어: 코드 · 셀#7
(A or B) and (C or D) => (A and C) or (A and D) or (B and C) or (B and D)

언어: 코드 · 셀#1
in

언어: 코드 · 셀#2
array-contains-any

언어: 코드 · 셀#3
OR

언어: 코드 · 셀#4
a in [1,2]

언어: 코드 · 셀#5
a = 1 OR a = 2

언어: 코드 · 셀#1
OR

언어: 코드 · 셀#2
AND

## orderBy 및 존재 여부
특정 필드를 기준으로 쿼리를 정렬할 때 쿼리는 정렬 기준 필드가 있는 문서만 반환할 수 있습니다.

예를 들어 다음 쿼리는 쿼리 필터를 충족하더라도 population 필드가 설정되지 않은 문서를 반환하지 않습니다.

관련 효과는 부등식에 적용됩니다. 필드에 부등식 필터가 있는 쿼리는 해당 필드를 기준으로 한 정렬도 포함합니다. 다음 쿼리는 해당 문서에 country = USA가 있더라도 population 필드가 없는 문서를 반환하지 않습니다. 이 문제를 해결하려면 각 정렬마다 별도의 쿼리를 실행하거나 정렬하는 모든 필드에 값을 할당할 수 있습니다.

위 쿼리는 부등식에 대한 묵시적 정렬 기준을 포함하며 다음과 같습니다.

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
population

언어: 코드 · 셀#1
country = USA

언어: 코드 · 셀#2
population

## 다음 단계
쿼리 결과에서 데이터의 순서를 지정하고 제한하는 방법 알아보기

결과 계산만 할 때 읽기 요금 절약하기