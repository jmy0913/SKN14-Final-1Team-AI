Source URL: https://firebase.google.com/docs/firestore/reference/rest/v1beta1/DocumentMask?hl=ko

## 본문
A set of field paths on a document. Used to restrict a get or update operation on a document to a subset of its fields. This is different from standard field masks, as this is always scoped to a Document, and takes in account the dynamic nature of Value.

### 표
| JSON representation |
| --- |
| JSON representation |
| { "fieldPaths": [ string ] } |

| Fields | |
| --- | --- |
| Fields | |
| fieldPaths[] | string The list of field paths in the mask. See Document.fields for a field path syntax reference. |

=== 코드/텍스트 탭 수집 ===
언어: 코드 · 셀#1
Document

언어: 코드 · 셀#2
Value