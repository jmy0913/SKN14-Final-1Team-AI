Source URL: https://cloud.google.com/firestore/docs/reference/rest/v1/projects.databases.userCreds/enable?hl=ko

## 본문
Enables a user creds. No-op if the user creds are already enabled.

POST https://firestore.googleapis.com/v1/{name=projects/*/databases/*/userCreds/*}:enable

The URL uses gRPC Transcoding syntax.

string

Required. A name of the form projects/{projectId}/databases/{databaseId}/userCreds/{userCredsId}

The request body must be empty.

If successful, the response body contains an instance of UserCreds.

Requires one of the following OAuth scopes:

https://www.googleapis.com/auth/datastore

https://www.googleapis.com/auth/cloud-platform

For more information, see the Authentication Overview.

### 표
| Parameters |
| --- |
| name | string

Required. A name of the form projects/{projectId}/databases/{databaseId}/userCreds/{userCredsId} |