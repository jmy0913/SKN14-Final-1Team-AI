Source URL: https://cloud.google.com/firestore/docs/reference/rest/v1/ListDocumentsResponse?hl=ko

## 본문
The response for Firestore.ListDocuments.

object (Document)

The Documents found.

string

A token to retrieve the next page of documents.

If this field is omitted, there are no subsequent pages.

### 표
| JSON representation |
| --- |
| {
  "documents": [
    {
      object (Document)
    }
  ],
  "nextPageToken": string
} |

| Fields |
| --- |
| documents[] | object (Document)

The Documents found. |
| nextPageToken | string

A token to retrieve the next page of documents.

If this field is omitted, there are no subsequent pages. |