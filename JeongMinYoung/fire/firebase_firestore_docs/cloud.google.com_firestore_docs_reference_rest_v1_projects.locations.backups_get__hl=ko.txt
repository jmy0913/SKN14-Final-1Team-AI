Source URL: https://cloud.google.com/firestore/docs/reference/rest/v1/projects.locations.backups/get?hl=ko

## 본문
Gets information about a backup.

GET https://firestore.googleapis.com/v1/{name=projects/*/locations/*/backups/*}

The URL uses gRPC Transcoding syntax.

string

Required. Name of the backup to fetch.

Format is projects/{project}/locations/{location}/backups/{backup}.

The request body must be empty.

If successful, the response body contains an instance of Backup.

Requires one of the following OAuth scopes:

https://www.googleapis.com/auth/datastore

https://www.googleapis.com/auth/cloud-platform

For more information, see the Authentication Overview.

### 표
| Parameters |
| --- |
| name | string

Required. Name of the backup to fetch.

Format is projects/{project}/locations/{location}/backups/{backup}. |